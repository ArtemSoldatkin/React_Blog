import Article from '../models/article'
import Review from '../models/review'

export default({   
    Mutation: {
        addReview: async(_, args, {userID}) => {
            try { } catch(err) {}
        }
    }
})
